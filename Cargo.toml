[package]
name = "q-lang"
version = "0.1.0"
edition = "2024"

[workspace]
resolver = "3"
members = ["lexer", "parser", "utils"]

[dependencies]
load_file = "1.0.1"
anyhow = "1.0"
lexer = { path = "lexer" }
parser = { path = "parser" }
utils = { path = "utils" }
